# The dsh-plugin-reloader bundle patch: one insert row, applied as a patch
# layer over the profile's plugin tree. Later patch layers address this row by
# id "plugin-reloader" to replace its config or disable it.

- insert:
    - id: plugin-reloader
      name: '@deepforce/dsh-plugin-reloader'
